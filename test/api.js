const chai = require('chai');
const mocha = require('mocha');

const api = require('../server/components/routes/api');

mocha.describe('testing api', () => {
  mocha.it('should do something', () => {
  });
});
